# vinculum-benchmark

This crate contains a simple frontend for benchmark purposes.

## Scripts

The `scripts` directory contains multiple scripts which where used to collect
the benchmark measurements and can be used to comprehend the methods used.
